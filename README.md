🤖 Утренний Telegram-бот
Бот, который каждое утро присылает гороскоп, курсы валют и новости.
Работает на Python, хранит данные в SQLite.
📦 1. Какие библиотеки нужны
Для работы бота используются следующие библиотеки:
aiogram — для работы с Telegram API.
requests — для запросов к сайтам.
feedparser — для чтения новостных лент.
beautifulsoup4 и lxml — для парсинга сайтов.
aioschedule — для расписания (рассылка по утрам).
(Библиотеки sqlite3 и os встроенные, их устанавливать не нужно).
⚙️ 2. Установка
Откройте терминал (консоль) в папке с проектом и выполните команду:
code
Bash
pip install aiogram requests feedparser beautifulsoup4 lxml aioschedule
🔑 3. Настройка
Откройте файл main.py в любом текстовом редакторе (Блокнот, VS Code).
Найдите строку (в начале файла):
code
Python
BOT_TOKEN = "ВСТАВЬ_СЮДА_СВОЙ_ТОКЕН"
Вставьте туда токен вашего бота, полученный от @BotFather.
▶️ 4. Запуск
Запустите бота командой:
code
Bash
python main.py
После запуска появится сообщение: 🚀 Бот запущен!.
Файл базы данных bot_database.db создастся автоматически.
